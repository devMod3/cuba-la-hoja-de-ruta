{
  "name": "cuba-la-hoja-de-ruta",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ZenBlog modular frontend for Cuba — La hoja de ruta",
  "scripts": {
    "test": "node --test",
    "check": "node --check dist/zenblog.js && node --check src/bootstrap/createZenBlog.js && node --check src/search/SearchService.js"
  },
  "engines": {
    "node": ">=20"
  }
}
